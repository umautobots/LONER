-
  mapper:
    optimizer:
      model_config:
        loss:
          loss_selection: L1_LOS
          depthloss_lambda: 0.0
          depth_eps_decay_rate: [0.85, 0.9, 0.95]
          min_depth_eps: [0.5]
