debug:
  # use_groundtruth_poses: [False]
  draw_rays_eps: [True]
  write_ray_point_clouds: [False]
mapper:
  optimizer:
    model_config:
      loss:
        dynamic_depth_eps_JS: [True, False]
        # depth_eps_decay_steps: [500]
        # visualize_loss: [True]
        # min_depth_eps: [0.03]
      # model:
        # occ_model:
        #   voxel_size: 1000 # default: 100
        #   lr: 0.001 # default: 0.0001

#         depth_eps_decay_steps: 50
        # visualize_loss: True
        # depth_lambda: [0]
        # term_lambda: [1.0]